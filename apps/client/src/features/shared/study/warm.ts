'use client';

/**
 * 扉が開いているあいだに、書斎を先に用意しておく。
 *
 * 書斎の canvas（three.js を使う側）は初期バンドルに載せず、書斎の画面に着いてから
 * 読み込んでいる（`study-home.tsx` の `dynamic`）。そのため**歩き切ってから読み込みが
 * 始まり**、その間は渡した 1 枚が静止したままになる。白くはならないが、動きが止まるので
 * 「ブツ切れ」に見える。
 *
 * ログインを押した時点で読み始めれば、扉が開いて歩くあいだ（約 1 秒）に間に合う。
 * 扉の側がすでに three.js 本体を読み込んでいるので、ここで増えるのは書斎ぶんだけ。
 *
 * **失敗しても何も壊さない。** 用意できなければ、書斎の画面で普通に読み込まれる。
 */
export function warmStudy(): void {
  void import('./components/study-canvas').catch(() => undefined);
}
